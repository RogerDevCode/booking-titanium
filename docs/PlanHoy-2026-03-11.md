# 📅 PLAN DE TRABAJO DETALLADO - PlanHoy-2026-03-11 (v1.3)

**Contexto Actual**: Backend blindado (v1.5.4), 100% QA exitoso, Identidad Persistente y Gestión de Concurrencia (Advisory Locks) activas.

---

## 🎯 OBJETIVO PRINCIPAL: MIGRACIÓN A AGENTE MCP (v3.0.0)
Migrar el enrutamiento basado en `Switch` y LLM Classifiers hacia una arquitectura de `AI Agent Node` con Tools (MCP), preservando estrictamente la seguridad y aislando el estado conversacional.

---

## 🛠️ PASO 0: DISEÑO DE CONTRATOS Y GUARDRAILS (Completado)
*(Integrado desde auditoría externa de diseño `NN_03-C_v3_Paso0_Contratos_Guardrails.docx`)*

### 0.1. Aislamiento de Memoria por Usuario
- **Mecanismo**: `Window Buffer Memory` configurado con `Session ID: ={{ $('Input_Clean').first().json.chat_id }}`.

### 0.2. Preservación del Pipeline de Seguridad
- **Flujo Intacto**: `Webhook` -> `Input_Clean` -> `Payload Validation` -> `Is Valid?` -> `Rule Firewall` -> `Is Safe?` -> **`AI Agent Node`**.

### 0.3. Diseño de Tools (`GetUserProfile` y `get_services`)
- `GetUserProfile` implementado como Tool Callable (lazy loading).
- `ConsultKnowledgeBase` es una **única tool** que envuelve el pipeline híbrido (SQL + RAG).

### 0.4. Resolución de Tools de Disponibilidad
- Mantenidas **separadas**: `CheckAvailabilityTool` y `FindNextAvailableTool`.

---

## 🏗️ PASO 1: CONVERSIÓN DE SUB-WORKFLOWS A TOOLS (Completado)
Aprovechamos los archivos `_v3.json` auditados que implementan el **Standard Contract Output v3.0.0** con un campo `message` pre-formateado. Despliegue de los 7 sub-workflows ejecutado exitosamente.

---

## 🧠 PASO 2: CONSTRUCCIÓN DEL NODO AGENTE (`NN_03-C_Agent_V3`)
Integración de la arquitectura validada por el auditor (`NN_03-C_Agent_V3.json`).

**Flujo de Despliegue:**
1. Reemplazar los placeholders de IDs en el JSON importado:
   - `<<JOIN_WAITLIST_ID>>` -> `FH8hRUv3dVgo8FB6`
   - `<<GET_USER_PROFILE_ID>>` -> `mJMUeVZSi55z8Bu9`
2. Subir el archivo `NN_03-C_Agent_V3.json` al servidor n8n usando `n8n_push_v2.ts`.
3. Validar visualmente en la interfaz que las 8 tools están conectadas al nodo Groq Llama 3.3 70B (T=0.1).

---

## 📝 PASO 3: INGENIERÍA DEL SYSTEM PROMPT MAESTRO (Integrado en V3)
El System Prompt ha sido inyectado directamente en el nodo AI Agent de `NN_03-C` con las 6 reglas estrictas, incluyendo la protección anti-alucinaciones para respuestas RAG vacías.

---

## 🧪 PASO 4: PRUEBAS DE INTEGRACIÓN AGÉNTICAS (E2E)
Crear un script `tests/qa-agent-tools.test.ts` para verificar:
1. **Memory Isolation**: Múltiples usuarios chateando simultáneamente sin mezclar contextos.
2. **Missing Parameters Guard**: Intento de reserva incompleto (esperar pregunta de vuelta).
3. **Cancellation Guard**: Verificar que el agente exija confirmación en dos pasos.
4. **Tool Error Handling**: Manejo del `USER_COLLISION`.
5. **RAG Fallback**: Preguntar por un seguro inventado y verificar la regla #4.

---

## ⚡ SIGUIENTES FASES (A encolar post-Agente)
- **Fase 2**: Implementación de Redis + BullMQ (Queue Mode) en Docker para absorber la carga del nuevo agente.
- **Fase 3**: Scaffolding del Frontend Administrativo (Next.js) consumiendo los endpoints de Audit Logs y Dashboards del DAL.
